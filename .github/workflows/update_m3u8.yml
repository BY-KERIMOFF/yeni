name: Update M3U Playlist

on:
  schedule:
    - cron: "*/15 * * * *"  # Hər 15 dəqiqədən bir işləsin
  workflow_dispatch:  # Manual başlatmaq üçün

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Repo-nu klonla
        uses: actions/checkout@v3

      - name: Python-u quraşdır
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Lazım olan kitabxanaları quraşdır
        run: pip install requests

      - name: M3U faylını yenilə
        run: python update_m3u.py

      - name: Dəyişiklikləri GitHub-a göndər
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add playlist.m3u
          git commit -m "🔄 M3U playlist avtomatik yeniləndi"
          git push
